<h1>Tasks</h1>
<%= render 'addbar' %>

<div class="container">
  <% @tasks.each do |task| %>
    <div class="row">
      <div class="col-md-3"></div>
      <div class="col-md-9"><%= link_to task.title, tasks_show_path(task) %></div>
    </div>
  <% end %>
</div>